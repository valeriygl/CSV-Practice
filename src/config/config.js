const tableHeaders = ["ID", "Name", "Description", "Rate"];
const filePath = "./user.csv";
const port = 3000;
const hostname = "localhost";

module.exports = {
  tableHeaders,
  filePath,
  port,
  hostname
};
